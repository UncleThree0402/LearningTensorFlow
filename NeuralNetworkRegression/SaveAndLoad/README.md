# Save And Load

## File

```python
#Save
model.save("name")
#Load (Return a model type)
tf.keras.models.load_model("name")
```

## HDF5
```python
#Save
model.save("name.h5")
#Load (Return a model type)
tf.keras.models.load_model("name.h5")
```